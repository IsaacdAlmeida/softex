// 4. Escreva uma função que retorne a área de um triângulo, a partir dos valores de base e altura fornecidos.

function calculaAreaTriangulo(base, altura) {
    area = (base * altura) / 2;
    return area;
}

console.log(calculaAreaTriangulo(10, 5));